interface:
  display_name: "TabRef Table Text Child Selector"
  short_description: "Find table-related body-text child blocks"
  default_prompt: "Use $tabref-table-text-child-selector to select the body-text child blocks that describe each table."
