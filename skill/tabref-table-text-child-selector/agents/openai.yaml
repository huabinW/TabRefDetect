interface:
  display_name: "TabRef Table Text Child Selector"
  short_description: "Find and refine table-related body-text child blocks"
  default_prompt: "Use $tabref-table-text-child-selector to select table-related body-text child blocks, preserving recall and separating code candidates, Codex review, and human gold."
